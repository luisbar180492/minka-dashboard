//View ids
const LOGIN = '/login';
const HOME = '/';

//Endpoints
const URL = 'https://pokeapi.co/api/';
const VERSION = 'v2';
const URL_PLUS_VERSION = `${URL}${VERSION}`;
const POKEMON_URI = '/pokemon/';

export {
  LOGIN,
  HOME,
  URL_PLUS_VERSION,
  POKEMON_URI,
};
